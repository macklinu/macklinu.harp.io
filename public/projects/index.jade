extends ../_layout

block content
  section.title
    h1 #{current.path[0]}
  for project, slug in public.projects._data
    if project.published
      a(href="./#{slug}")
        section.card
          h1 #{project.name}
          h2 #{project.tidbit}
          img(src="#{project.img}")